package santa_cruz_alimento_backend.controller;

/*

@RestController
@RequestMapping(API)
public class SubProductController {

    @Autowired
    private ISubProductService subProductService;

    @PostMapping(SUBPRODUCT)
    public BaseResponse save(@RequestBody SubProductRequestDto requestDto){
        var response = subProductService.save(requestDto);
        return new BaseResponse(true, response, MESSAGE_SAVE);
    }

    @GetMapping(BY_SUBPRODUCT_ID)
    public BaseResponse getById(@PathVariable Long id){
        var response = subProductService.getSubProductById(id);
        return new BaseResponse(true, response, MESSAGE_BY);
    }

    @PutMapping(BY_SUBPRODUCT_ID)
    public BaseResponse updateById(@PathVariable Long id, @RequestBody SubProductRequestDto requestDto){
        var response = subProductService.updateBySubProductId(id, requestDto);
        return new BaseResponse(true, response, MESSAGE_UPDATE);
    }
}
*/
